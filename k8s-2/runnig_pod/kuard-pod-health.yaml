apiVearion: v1
kind: Pod
metada:
  name: kuard
spec:
  container:
    - image: gcr.io/kuard-demo/kuard-amd64:1
      name: kuard
      livnessProbe:
        httpGet:
          path: /healthy
          port: 8080
        initialDelaySeconds: 5
        timeoutSeconds: 1
        periodSeconds: 10
        failureThreshold: 3
      ports:
        - containerPort: 8080
          name: http
          protocol: TCP